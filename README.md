#### OSCAR

Subprojects:
1) downloader
2) controller
3) fossology
4) ort

### To start
```
git submodule deinit --all -f
git submodule init
git submodule update

docker-compose build
docker-compose up
```